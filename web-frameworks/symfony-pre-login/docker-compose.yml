services:
  target:
    build: 
      context: target
  attacker:
    build: attacker
    environment:
      PROTOCOL: "http"
    depends_on:
      - target
  server:
    build: server
    ports:
      - 80:80
      - 443:443
    depends_on:
      - target
      - attacker

